# CrewChief 中文语音包制作方案

> 目标游戏：**Assetto Corsa**（AC）
> 制作工具：**crew-chief-autovoicepack**（已 fork 改造，方案已实测验证）
> 文档状态：技术路线已跑通，等待铺开到全量

---

## 〇、三个仓库的分工

制作工程独立成库，另外两个只读引用、需要时才改：

| 角色 | 路径 | 说明 |
|---|---|---|
| **制作工程**（本库） | `~/Projects/CrewChief_CN_VoicePack` | 翻译语料、baseline、生成脚本、补丁、文档的**唯一权威来源**，git 管理 |
| 生成工具 | `~/Projects/crew-chief-autovoicepack` | 提供 Python venv 与 XTTS 运行环境。对它的改动以 diff 形式留档在本库 `patches/` |
| 目标程序 | `~/Projects/CrewChiefV4-main` | CrewChief 源码，只读参考。`NumberReaderZh` / `ColloquialTime` 改动最终落在这里 |

```
CrewChief_CN_VoicePack/
├── docs/            方案与设计文档（本文件）
├── translations/    翻译语料 CSV  ← 核心资产
├── baseline/Bart/   定稿音色参考音频（12 条 / 38.9s）
├── scripts/         生成脚本
├── patches/         对 autovoicepack 的改动留档
└── output/          生成产物（.gitignore 排除）
```

**同步方向：本库 → autovoicepack。** 在 autovoicepack 里临时改了脚本或语料，记得用 `git diff > patches/...` 回写本库，别让两边各自演化。

---

## 一、结论摘要

| | |
|---|---|
| 可行性 | ✅ 已验证。CrewChief 架构原生支持多语言语音包（已有意/巴葡先例），语音文件夹名是语言无关标识符，**换音频不改播报逻辑** |
| 生成方案 | XTTS v2 音色克隆，**Bart** 音色 + `zh-cn` + 语速 1.45x + radio EQ |
| 必做音频量 | **3882 条去重文案 / 5819 个音频文件 / 约 143 分钟** |
| 机器时间 | 约 **3 小时**（M1 实测 1.6–2.7 秒/条） |
| 代码改动 | autovoicepack 3 个文件（**已完成**）+ CrewChief 2 处（`NumberReaderZh`、`ColloquialTime`） |
| 主要瓶颈 | **工程师播报 3772 条文案的翻译**，不是音频生成 |
| 许可限制 | ⚠️ XTTS v2 为 **Coqui CPML，非商用**。免费开源分发可以，但有三个硬条件，见 §13 |

---

## 二、已验证的生成方案

### 2.1 定稿参数

```bash
scripts/pilot_mac.py \
  --phrase_inventory <翻译后的CSV> \
  --language zh-cn \
  --baseline_audio_dir baseline/Bart \
  --xtts_speed 1.45 \
  --eq_preset radio \
  --voice_name <输出名>
```

> 完整可执行命令见 §12。

| 参数 | 值 | 依据 |
|---|---|---|
| 音色 | **Bart** | ElevenLabs 合成音色，**不涉及真人声纹**。从官方 1.2GB 完整包 30714 个文件里筛出 12 条干净片段，共 **38.9 秒** baseline |
| 语言码 | `zh-cn` | XTTS v2 官方支持的 17 语言之一 |
| 语速 | **1.45x** | 实测 **188ms/音节**，普通话正常语速区间（170–200ms）。1.6x（149ms）偏快，长句压到 132ms 会糊 |
| EQ | **radio** | 原项目曲线，试听确认 |
| 变体数 | 1（`--variation_count 0/1`） | CrewChief 会在同文件夹内轮换变体，双变体可减少机械感，代价是时间翻倍 |

### 2.2 音频格式（与 CrewChief 语音包一致）

**单声道 / 22050 Hz / 16-bit PCM**，峰值归一到 −1 dBFS，首尾静音已裁。

### 2.3 被否决的方案及原因

| 方案 | 结果 | 原因 |
|---|---|---|
| macOS 原生中文 TTS 做 baseline | ❌ 更差 | 共振峰合成的机械感被一起克隆进去；`收到` 出现 576ms/音节 的拖音异常 |
| CrewChief 自带真人语音做 baseline | ❌ 放弃 | 效果不佳，且涉及署名个人（Geoffrey Lessel、Matt Orr 等）的声纹 |
| warm / full / broadcast EQ | ❌ 未采用 | 试听后选定 radio |
| baseline 从 14 秒扩到 39 秒 | ⚠️ 收益有限 | 音高标准差仅从 37.0Hz 降到 32.7Hz（−12%），XTTS conditioning 在 15 秒左右接近饱和。但成本已付，保留 |

---

## 三、工作分解

### 3.1 必做项

| # | 工作项 | 量 | 类型 | 状态 |
|---|---|---|---|---|
| 1 | **工程师播报** | 1210 文件夹 / 5617 音频 / **3772 条文案** / 140min | 翻译 + TTS | 未开始 |
| 2 | **Spotter 播报** | 17 文件夹 / 124 音频 / **58 条文案** / 1.6min | 翻译 + TTS | ✅ 完成 |
| 3 | **radio_check** | 1 文件夹 / **4 条** | 翻译 + TTS | ✅ 完成 |

> radio_check 只需要基础包的 `voice/radio_check/test/`（chief 与 spotter 共用，[AudioPlayer.cs:758](CrewChiefV4/Audio/AudioPlayer.cs:758) 与 [NoisyCartesianCoordinateSpotter.cs:201](CrewChiefV4/NoisyCartesianCoordinateSpotter.cs:201)）。另外 12 个 `radio_check_<人名>/` 是备选音色专用，与 `spotter_*` 一样跳过。应答音 `acknowledge/radio_check` 已包含在工程师那 3772 条里。
| 4 | **数字音频** | **138 个文件夹**（英文 1052，中文可拼接） | TTS | ✅ 完成 |
| 5 | **`NumberReaderZh`** | 10 个抽象方法 | **C# 代码** | ✅ 完成，见 [数字与时间朗读设计](数字与时间朗读设计.md) |
| 6 | **`ColloquialTime` 中文化** | 60 个映射 | **C# 代码** | 未开始 |
| 7 | **UI 中文化** | **2222 条**（含 469 条 tooltip/help） | 翻译 | 未开始 |
| 8 | **字幕字体** | 1 个设置 | 配置 | ⬜ 改法已定（改 json，不用编译），只差在 Windows 上确认默认字体够不够用 |
| 9 | **打包与防覆盖** | 3 处配置 | 配置 | ✅ 完成，装法写进 [INSTALL.txt](../packaging/INSTALL.txt)，打包脚本 [build_release.sh](../packaging/build_release.sh) |
| 10 | 语音指令 | 30–50 个高频指令 | 翻译 + 装引擎 | 可后置 |

**①+②+③ 合计：1240 文件夹 / 5819 音频 / 3882 条去重文案 / 143 分钟**

### 3.2 明确跳过

| 内容 | 量 | 理由 |
|---|---|---|
| 弯角名 corners | 761 文件夹 / 598 文案 | Eau Rouge、130R、Parabolica——中文车手也说原名 |
| 车手名 driver_names | 11174 音频 | 保留英文原包，架构支持混用 |
| 个性化称呼 personalisations | 22718 音频 / 946 个英文名 | 中文场景无用 |
| Spotter 备选音色 | 182 文件夹 / 2064 音频 | 12 个备选人声，只需默认那套 |
| 拉力领航员 codriver | 627 文件夹 / 419 文案 | AC 非拉力模式不加载 |
| 音效/背景音 fx | 16 音频 | 无人声 |
| 帮助文档 | 80 个 HTML | 可选，不影响使用 |
| 车辆组别号 GT3/LMP2/DTM 等 | 34 文件夹 / 103 音频 | 专有编号，中文车手说原名；且中文 TTS 念拉丁字母不可靠，见 §9 |
| 组别编号 Group A/C/5 等 | 8 文件夹 / 20 音频 | 同上 |
| 超车辅助 overtaking_aids | 18 文件夹 / 79 音频 | 整个分类都是 DRS / KERS / push-to-pass，同上 |

**跳过合计约 32490 个音频文件 / 460 分钟**——这是「只做必要部分」相比全量的节省。

### 3.3 工程师播报的内部构成（3772 条文案）

| 模块 | 文案 | 模块 | 文案 | 模块 | 文案 |
|---|---:|---|---:|---|---:|
| tyre_monitor 轮胎 | 399 | multiclass 多级别 | 193 | race_time 赛时 | 56 |
| penalties 罚则 | 348 | damage_reporting 损伤 | 162 | overtaking_aids | 52 |
| mandatory_pit_stops 进站 | 337 | lap_counter 圈数 | 140 | driver_swaps 换手 | 34 |
| flags 旗语 | 331 | fuel 燃油 | 131 | engine_monitor 引擎 | 33 |
| position 位置 | 230 | battery 电量 | 102 | rants 抱怨 | 30 |
| lap_times 圈速 | 227 | frozen_order 队列 | 100 | pace_notes | 14 |
| timings 差距 | 198 | watched_opponents 盯人 | 80 | incidents | 7 |
| acknowledge 应答 | 196 | strategy 策略 | 79 | alarm_clock | 7 |
| | | opponents / conditions / pearls_of_wisdom / push_now | 280 | licence | 6 |

---

## 四、AC-only 带来的简化（已验证）

[CrewChief.cs:390-413](CrewChiefV4/CrewChief.cs:390) 按游戏裁剪事件模块，AC 会移除：

| 被移除的模块 | 收益 |
|---|---|
| **`PitManagerVoiceCmds`**（仅 RF2/LMU 加载） | ✅ **`SpokenNumberParser` 无需改造**。它按空格切词 + 硬编码英文数词，中文完全失效，但全项目仅 3 处调用且全在 PitManager。AC 不加载 → 省掉这处代码改动 |
| `IRacingBroadcastMessageEvent` | iRacing 专属指令不用翻译 |
| `Ratings`（iRacing/R3E 专属） | iRating / 安全等级相关不用翻译 |
| `CoDriver`（拉力专属） | 419 条路书文案不用做 |

⚠️ **注意**：语音指令的**语法本身基本不分游戏**——[SpeechRecogniser.cs](CrewChiefV4/SpeechRecogniser.cs) 里只有 6 处 `Game.XXX` 判断。所以「只做 AC」对指令翻译量减少有限，主要省的是代码改动和拉力部分。

---

## 五、代码改动清单

### 5.1 autovoicepack 侧（✅ 已完成，改动只留在工作区，未提交）

| 文件 | 改动 | 原因 |
|---|---|---|
| `generate_voice_pack.py` | `model.inference(text, "en", ...)` → `language` 参数化，新增 `--language`（17 语言校验） | **语言码原本硬编码为 `en`**，中文文本走英文 tokenizer 出不了可用结果 |
| " | 新增 `--speaking_rate`（CJK 默认 5.0） | 质检的语速常数按拉丁字母校准；沿用 3.0 会让检查永不触发，AI 跑飞的音频直接混进包里 |
| " | 新增 `--silence_check_text_length`（CJK 默认 12） | 静音容忍度原按 30 字符分档，中文句子字符数少得多 |
| " | 新增 `--eq_preset`（radio/warm/full/broadcast） | 原曲线只留 4.2% 低频能量，TTS 输出叠上去会「空洞」 |
| " | `open(..., encoding="utf-8")` | `subtitles.csv` 和安装说明原本未指定编码，非 ASCII 会乱码 |
| " | 非英语时跳过内置英文替换规则 | `\bmate\b`、`130R → one-thirty R` 等规则对中文无效且可能污染 |
| " | 中文 radio_check 短语表（21 条） | 原表全英文硬编码 |
| " | 非英语包追加安装说明 | 见 §7 的 alt 陷阱 |
| `translate_phrases.py` | `--exclude_paths`（默认排除 corners/numbers/codriver） | 见 §3.2 |
| " | `--only_paths` | 支持小批量试点 |
| " | 翻译缓存 + 断点续跑时从已有 CSV 回填 | 5244 行里只有 3701 条不重复，省 29% LLM 调用 |
| " | `YOUR_NAME` 占位符保护 + 丢失告警 | LLM 常把占位符一起翻译掉 |
| " | prompt 里带上消息类别（`lap_counter/green_green_green`） | 原本只喂一句孤立英文，`green, green, green` 译成颜色而不是绿灯 |
| " | 新增 `--glossary_file` | 赛车术语是机翻最稳定出错的地方，且**错得不随机**：`2 tenths` 每次都出「百分之二十」 |
| " | 新增 `--temperature`，默认 0.4 → **0.2** | 0.4 下同一句在两次运行里能给出「绿色」和「绿灯」两种结果 |
| " | prompt 加「按字面译，不要意译加词」 | 给了术语表后模型转向另一个极端，把 `green, green, green` 演绎成「起跑，起跑，起跑」 |
| " | `--ollama_host` / `--ollama_timeout`（默认 60s） | 原 host 写死、超时 20s 对大模型不够 |
| `Dockerfile` | 加装 `jieba` `pypinyin` | XTTS 的 `zh-cn` 分支依赖，实测确实缺失 |
| `pilot_mac.py`（新增） | 脱离 Docker/CUDA 的本地生成脚本 | Docker 镜像是 amd64+CUDA，Apple Silicon 跑不了 |

本库自己的三个脚本这一轮也改了（不涉及 autovoicepack，无需回写补丁）：

| 脚本 | 改动 | 原因 |
|---|---|---|
| `make_chief_inventory.py` | `SKIP_EXACT` 加 `overtaking_aids`；新增 `DROP_CLASS_PATTERN` 排除组别号与 Group 系列 | 见 §9 的判断标准 |
| " | 新增 `DROP_OVAL_PATTERN` 排除 stock car 程序 | `StockCarRulesData` 只有 iRacing/RF2 的 mapper 会填，AC 不触发 |
| `fix_translation.py` | `--corrections` 接受多个文件，只对最后一个报未命中 | 修正表要跨波次继承，否则波次 2 定的口径到波次 3 全丢 |
| " | `TERM_FIXES` 加全局去空格（拉丁↔汉字之间） | XTTS 在空格处断句吞后半段 |
| " | 数字归一化正则加数字前瞻 `(?<![A-Za-z\d])` | 原来把 `GT300` 末尾的 `00` 转成「零」，变成「GT3零」 |
| `qa_pack.py` | 音节计数支持拉丁：单字母 1 音节，缩略词 2 音节 | 原来只数汉字，含 DRS/KERS 的条目全部虚报 |

### 5.2 CrewChief 侧

补丁：[`patches/crewchief-zh-numberreader.diff`](../patches/crewchief-zh-numberreader.diff)，新文件在 [`src/NumberReaderZh.cs`](../src/NumberReaderZh.cs)。

| 文件 | 改动 | 状态 |
|---|---|---|
| `NumberReaderZh.cs`（新建） | 实现 `NumberReader` 的 10 个抽象方法 | ✅ 完成 |
| `NumberProcessing/NumberReaderFactory.cs` | 加 `zh` / `zh-cn` 分支 | ✅ 完成 |
| `CrewChiefV4.csproj` | 编译新文件 | ✅ 完成 |
| `UnitTest/Misc/TestNumbersInMessages.cs` | 34 条中文用例 | ✅ 完成 |
| `ColloquialTime.cs` | 中文化 60 个映射 | 未开始 |

> 原先记的关键风险是：英文包用 **809 个 `X point Y` 预录文件**保证圈速韵律，中文「1分23秒4」结构完全不同，
> 拆错了会得到「一 点 二三」这种听不懂的结果。实际结论是中文数字每个字读音固定、可以纯拼接，
> 138 个文件夹就够，风险解除。完整规则见 [数字与时间朗读设计](数字与时间朗读设计.md)。
>
> `sounds_config.txt` 里那几个数字组合开关（`say_and_between_hundred_and_units` 等）只被 `NumberReaderEn`
> 读取，中文实现不看，**不需要改**。

---

## 六、翻译方案

### 6.1 语料来源

| 来源 | 覆盖 | 说明 |
|---|---|---|
| autovoicepack `phrase_inventory.csv` | 9145 行 / 3273 文件夹，覆盖当前包 **84%**（3261/3862） | 对着旧版语音包做的，缺新增内容 |
| CrewChief 自带 `subtitles.csv` | 2956 个文件 / 10415 行，**100%** | 每个语音文件夹都有 `wav名,"英文原文"`，是权威台本。`extra/generate_phrase_inventory.py` 可从中生成 inventory |

**建议用后者重新生成 inventory**，保证覆盖完整。

### 6.2 翻译执行

排除 corners/numbers/codriver 后：**5244 行 → 3701 条不重复文案**（缓存生效后的实际 LLM 调用量）。

```bash
python3 translate_phrases.py \
  --target_language "Simplified Chinese" \
  --model qwen2.5:14b \
  --translated_phrase_inventory $PACK/translations/phrase_inventory_zh.csv \
  --skip_sanity_check
```

⚠️ **不要用 qwen2.5:32b**：19GB 模型在 16GB 内存的 M1 上会疯狂 swap。用 `qwen2.5:14b`（约 9GB）或云端 API。

### 6.3 术语表（已确认）

| 英文 | 中文 | 备注 |
|---|---|---|
| 3 wide | **三辆并排** | 不用「三宽」 |
| clear / all clear | **安全** / 都安全 | 不用「空了」 |
| inside / outside | 内线 / 外线 | 走线位置 |
| low / high | 内侧 / 外侧 | 相对高低 |
| box now | 进站 | |
| safety car | 安全车 | |
| hold your line | 保持走线 | |

翻译完必须人工过一遍赛车术语，机器翻译在这块不可靠。

### 6.4 语料 CSV 格式

沿用 autovoicepack 的 inventory 格式，5 列，UTF-8：

```csv
audio_path,audio_filename,subtitle,text_for_tts,original_english
\voice\spotter\car_left,1.wav,左边有车,左边有车,car left
```

| 列 | 含义 |
|---|---|
| `audio_path` | 输出路径，**每一段都是 CrewChief 播报逻辑里的标识符** |
| `audio_filename` | 同一文件夹内的变体编号，CrewChief 轮换播放 |
| `subtitle` | 字幕文本，写入 `subtitles.csv`，覆盖层显示用 |
| `text_for_tts` | 送进 XTTS 的文本，通常与 `subtitle` 相同 |
| `original_english` | 英文原文，人工校对时对照用，不参与生成 |

⚠️ **文件夹名不能翻译。** `audio_path` 的每一段（`spotter/car_left`、`numbers/ten_thousand`）都要与英文包的目录结构**逐字对应**——那是播报逻辑里的标识符，不是描述性名字。写错了这条消息永远不会播，而且不报错。只有 `subtitle` 和 `text_for_tts` 是中文。

`YOUR_NAME` 是占位符，生成时被 `--your_name` 替换。翻译时不能连它一起译掉——LLM 经常这么干，`translate_phrases.py` 为此加了占位符保护与丢失告警（见 §5.1）。

---

## 七、安装与防覆盖

### 7.1 ⚠️ 不要装到 `alt/` 目录

CrewChief 在使用非默认工程师语音包时，会**强制**从基础包读取 `spotter*` 和 `radio_check*`（[Sounds.cs:1147-1162](CrewChiefV4/Audio/Sounds.cs:1147)）：

```
选了 alt 语音包 + 公路赛：
  spotter* / radio_check*  →  强制从【基础包】读（英文）
  其余所有 voice 文件夹     →  从 alt 包读
```

按 autovoicepack README 的装法（拷到 `Sounds\alt\`），结果是**工程师说中文、spotter 永远说英文**——而 spotter 恰恰是比赛中出现频率最高的播报。

### 7.2 正确装法

```
1. 复制基础语音包目录             %LOCALAPPDATA%\CrewChiefV4\Sounds  →  Sounds_zh
2. 把中文 voice/ 覆盖上去
3. 设置 override_default_sound_pack_location = <Sounds_zh 路径>
4. chief_name 保持默认（不触发 alt 重定向）
5. sounds/sound_pack_language.txt 内容改为  zh   （仅打了补丁的构建，见下）
6. 没写第 5 步的话，sounds/sound_pack_version_info.txt 版本号设成很大的值
```

这样整包替换，spotter 也是中文。

**防覆盖的实际机制**（写进 INSTALL.txt 的「关于自动更新」一节，措辞对使用者更直白）：

CrewChief 不会静默覆盖，它只把语音包下载按钮点亮，用户点了才下载。按钮亮不亮看
`latest > current`（[SoundPacks.cs:117](CrewChiefV4/UserInterface/SoundPacks.cs:117)），`current` 取自语音包根目录的
`sound_pack_version_info.txt`，`latest` 按**语言**从上游 XML 取：

| 语言标记 | 取到什么 | 结果 |
|---|---|---|
| `zh`（第 5 步） | 上游 XML 没有 zh 段，`voiceMessageUpdatePacks` 为空 | 按钮永远灰，天然免疫 |
| 空（原版程序） | [MainWindow.cs:607](CrewChiefV4/UserInterface/MainWindow.cs:607) 的 `soundPackLanguage == null ? "en"` 回落到 en，拿到真实英文版本号 | 上游一发新版按钮就绿，需要第 6 步撑大 `current` |

⚠️ `-skip_updates` **不能**当防线。它只跳过 `AutoUpdater.Start()`（程序本体更新），语音包的
XML 照样下载解析（[MainWindow.cs:598-608](CrewChiefV4/UserInterface/MainWindow.cs:598)）。

### 7.3 其他配置

| 项 | 值 | 原因 |
|---|---|---|
| `ui_language` | `zh` | 加载 `ui_text/zh.txt`（`Configuration` 自动合并到 `en.txt` 之上） |
| Overlay `fontName` | 微软雅黑等有中文字形的字体 | 默认是 `Microsoft Sans Serif`（[OverlaySettings.cs:39](CrewChiefV4/Overlay/OverlaySettings.cs:39)），无中文字形时字幕显示方块。改 `我的文档\CrewChiefV4\subtitle_overlay.json` 与 `crewchief_overlay.json`，**普通 JSON，重启生效，不用重新编译** |
| `speech_recognition_config.txt` 的 `language` | `zh` | 仅做语音指令时需要 |
| `speech_recognition_country` | `CN` | 同上 |

---

## 八、语音指令（可后置）

**不需要录任何音频**——这是识别（输入），与播报完全解耦。但也不只是翻译：

| 层面 | 内容 | 量 |
|---|---|---|
| 翻译 | `speech_recognition_config.txt`：**423 个指令 key / 781 条短语** + 1099 个数字 key / 1498 条短语 | 数字部分高度规律，可脚本生成 |
| 引擎 | 装 `MSSpeech_SR_zh-CN_TELE.msi`（MS Speech Platform）或 Windows 中文语音识别包 | 找不到会弹窗报错 |
| 代码 | ✅ AC 无需改动（见 §4） | |

⚠️ **现实问题**：这套架构的语法是「短语列表精确匹配」（`GrammarBuilder` + `Choices`）。中文同音字极多（进站/近站、四/是），423 个指令 × 多个变体全塞进一个语法，误识别率会显著上升。

**建议只保留 30–50 个高频指令**，而不是全量翻译 781 条。配置文件注释本身也提到删减指令能大幅提升识别质量。

---

## 九、分阶段计划

### 第一阶段：能听到完整中文（2–3 天）

```
① Spotter 124 条            ✅ 翻译 + 生成
② radio_check 4 条          ✅ 翻译 + 生成
   装包实测                 ✅ AC 中确认无线电测试与盲区提醒都是中文，
                               证明「替换基础包 + override_default_sound_pack_location」这条路可用
③ NumberReaderZh + 138 条数字音频   ✅ 设计、C# 实现、单元测试、生成
④ 打包配置                   ✅ INSTALL.txt + build_release.sh，v0.1.0 已发布
⑤ 字幕字体                   ⬜ 唯一还要 Windows 的事：开字幕覆盖层看一眼中文
                                是不是方块。是的话改 subtitle_overlay.json 的
                                fontName，改法已写进 INSTALL.txt
```

产出：装进 AC 能跑一圈，盲区提醒、进站、旗语、圈速全中文。

### 第二阶段：主体（1.5–2 周）

工程师播报。**按波次切分，每波独立可交付**——装进 AC 就能听到进展，不必等全部做完。

准确工作量（直接走 CrewChief 自带 `subtitles.csv` 统计，比早期估的 3772 更实）：

| 波次 | 分类 | 最终音频 | 人工修正 | 状态 |
|---|---|---:|---:|---|
| **1** | flags、lap_times、position、acknowledge、timings、lap_counter、fuel | 1921 | 407 | ✅ 已完成 |
| **2** | tyre_monitor、penalties、damage_reporting、conditions、opponents、push_now、race_time | 1629 | 212 | ✅ 已完成 |
| **3** | mandatory_pit_stops、multiclass、battery、frozen_order、watched_opponents、strategy、driver_swaps、engine_monitor | 1341 | 244 | ✅ 已完成 |
| **4** | pearls_of_wisdom、rants、incidents、alarm_clock、licence | 151 | （与波次 3 合并审校） | ✅ 已完成 |
| | **合计** | **5042** | | ✅ **第二阶段完成** |

四波实际产出 **5042 条工程师语音**（另有 202 条按上面的判断标准回落英文），加上已发布的
Numbers 138 + Spotter 128，语音包共 **5308 条 / 1264 文件夹 / 323 MB**。
人工修正合计 **863 条**（波次 1 的 407、波次 2 的 212、波次 3+4 的 244）。

六批 `qa_pack.py` 全部 **异常 0 条**，字幕逐条校验 0 条不一致。

#### 一条判断标准：英文缩写主体的播报不做，回落英文

语音包覆盖在英文包副本上，**不发的文件夹自动播原来的英文**，所以「不做」是可用选项而不是缺失。

这一轮在五个地方连续撞到同一个根因——**中文 TTS 念拉丁字母不可靠**：

| 现象 | 实测 |
|---|---|
| `bar` / `PSI` 被 QA 判异常 | 单音节拉丁词时长落在中文阈值外 |
| `GTC` 三个字母 | **1.73 秒**，`--attempts 8` 重跑无效，稳定复现 |
| `DTM赛车` | 截断到 **0.21 秒**（光两个汉字也要 0.34 秒） |
| `DRS 不再可用` | **0.49 秒**，比四个汉字单独念还短 |
| `DRS` 在句中 | 只占 **0.25 秒**，三个字母名至少要 0.5 秒 |

于是把这类整体排除，共 **60 个文件夹 / 202 条音频**：车辆组别号（GT3/GT300/LMP2/DTM/Carrera Cup）、
组别编号（Group A/C/5）、`overtaking_aids` 整个分类（DRS/KERS/push-to-pass）。加上原有的车手名和
弯角名，判断标准统一为：**主体是英文缩写或专有编号的，不做**。

排除规则在 `make_chief_inventory.py` 的 `SKIP_EXACT` 与 `DROP_CLASS_PATTERN`，重新切语料自动生效。

⚠️ **拉丁字母与汉字之间不能留空格**，XTTS 会在那里断句并吞掉后半段。`fix_translation.py` 的
`TERM_FIXES` 有一条全局去空格规则。

#### `qa_pack.py` 的音节计数改了三版

原来只数汉字，把拉丁字母当 0 音节，含 DRS/KERS 的条目全部虚报「过长」。改成逐字母计数后又矫枉过正，
把 `别忘了用KERS` 报成「过短」。靠对照实验定的案：

| 文本 | 时长 |
|---|---:|
| `别忘了用` | 0.55s |
| `别忘了用KERS` | 0.85s |
| `别忘了用凯尔斯` | 1.03s |

KERS 确实发音了（多 0.30 秒），但 XTTS 把它当**一个词**而不是逐字母拼读。最终规则：单个字母算
1 音节，缩略词整体算 2 音节。

`scripts/make_chief_inventory.py` 负责切分，它顺手做了三件 autovoicepack 的
`extra/generate_phrase_inventory.py` 没做的事，命令见 §12：

- 补上 `\voice\` 前缀 —— 缺了它 `translate_phrases.py` 的 `--exclude_paths` / `--only_paths` 全部失配，
  排除规则静默失效
- 补第 5 列 `original_english`
- 剔掉 24 个备选音色分类和已完成的 spotter / radio_check / numbers

**每波固定六步：**

```
1. 切语料   make_chief_inventory.py --wave <N>          英文待译
            make_chief_inventory.py --wave <N> --manual  官方无台本、已手写好的中文
2. 翻译     translate_phrases.py --glossary_file translations/glossary_zh.txt
3. 人工审查  结果写进 translations/chief_wave<N>_corrections.csv
4. 应用修正  fix_translation.py（术语归一化 → 人工修正 → 数字归一化）
5. 合并     tail -n +2 <N>_manual_zh.csv >> <N>_final.csv
6. 生成体检  pilot_mac.py --voice_name ChiefW<N>，然后 qa_pack.py
```

第 3 步是唯一的人工环节，也是真正的瓶颈。**修正表按 `original_english` 索引**，所以换 prompt 重新翻译后照样生效，而且跨波次通用——波次 1 定下的术语口径会自动套到后面几波。

#### 机翻实际错在哪（波次 1 实测）

1452 条里改了 404 条。错误不是随机分布的，按危险程度排：

| 类型 | 例 | 后果 |
|---|---|---|
| **反义** | `safety car in`（进站）→「出现」；`gone off`（冲出赛道）→「脱困」；`reeling you in`（追近）→「套你圈」 | 车手照做就是反向操作 |
| **同一术语多种译法** | `spotter` 在 acknowledge 一个分类里出现 8 种译法，含「报站」（公交车报站名）、「领航员」（那是拉力 codriver） | 听起来像换了个人在说话 |
| **量纲错** | `2 tenths`（零点二秒）→「百分之二十」 | 数值完全错 |
| **片段直译** | `laps ahead` →「圈数领先」，中文量词必须跟在数字后面才拼得出「你三圈领先」 | 拼出来不成句 |
| **书面语** | `go, go, go` →「去，去，去」 | 喊不出口 |

前三类靠通读发现，后两类靠脚本扫描（残留数字、省略号、悬空逗号、繁体字、中文夹空格）。**两种都要做，各自会漏掉对方那一半。**

#### 波次 1 生成实测

1921 条 / 4784 秒（2.5 秒/条）。`qa_pack.py` 报出 **9 条异常**，全是 2–5 音节的短句，6 条过短
（最极端的 `你进来了` 只有 0.13 秒 / 4 音节 = 33 ms）、3 条过长。

根因是这批用了默认的 `--attempts 1`：`pick_best_candidate` 有 `--min_ms_per_syllable`（默认 90）这道
闸，但只有一个候选时它没得选，只能把不合格的返回。**短句必须开多候选**：

```bash
--attempts 5 --attempts_max_syllables 0
```

对这 9 条重跑后异常清零，语速范围从 33–548 ms/音节收敛到 93–419。整批只花 66 秒，所以更省事的做法是
一开始就对短句开多候选，而不是事后补。

⚠️ **重跑之后必须重建 `subtitles.csv`**，原因见 [CLAUDE.md](../CLAUDE.md) 里那条约束。

#### 波次 2 实测：术语表要在开跑前配齐

波次 2 第一次跑到 294 条时发现，它的高频词几乎全不在术语表里——`penalty` 117 处、`track limits` 98 处、
`stop-go` 41 处、`serve` 33 处、`locking` 28 处，加起来 300 多处靠模型自由发挥。**停掉重跑**，术语表从
45 条扩到 82 条。代价 85 分钟机器时间，换来的一致率：

| 术语 | 一致率 |
|---|---|
| `track limits` → 赛道界限 | 65/66 |
| `wear` → 磨损 | 29/29 |
| `pressures` → 胎压 | 20/20 |
| `locking` → 抱死 | 25/28 |

对照波次 1 的 `spotter` 出现 8 种译法，这次没有同类问题。**开跑前先扫一遍语料的高频术语**，比事后写几百条修正划算得多。

⚠️ **术语表按无线电口语选词，不是按词典。** 我一开始写 `aero = 空气动力套件`，结果满屏「一些轻微的
空气动力套件损坏」——工程师不会这么喊。改成「空力」。

#### 波次 2 的错误类型（1173 条改了 212 条）

前一波已知的反义错、片段直译、术语不一致照旧，另外新增三类：

| 类型 | 例 | 为什么难发现 |
|---|---|---|
| **单位当成别的东西** | `bar`（压力单位巴）→「轮胎监测」；`psi` →「胎压」 | 译文本身像个正经术语 |
| **原文 typo 被顺着译** | `get some head into these brakes`（head 是 heat 笔误）→「给刹车**降温**」 | 意思正好反了 |
| **同义词全译成一个词** | hypersoft / ultrasoft / supersoft 三种配方都叫「超软胎」 | 单看每条都对 |

还有一条 `cooking`（过热）被系统性译成「磨损」共 14 条，「你的左前胎在磨损」读起来完全通顺——
**译文越通顺越难发现，只能对着原文逐条读。**

#### 波次 3+4：两条新规矩

**① 组别名不能翻译，跟弯角名同理。** GT3、GTE、Group C、LMP2、DTM 这些是专有名词，中文车手也说原名。
机翻的处理五花八门：`GTC` 和 `GTE` 都译成「GT」——**尾字母丢失，两个不同组别变成同一个**；
`Group 6` 译成「组别」，数字没了。共 61 条改回原文。

**② 修正表要跨波次继承。** 波次 2 定的「空力」「标准胎」口径到了波次 3 全丢了，`aero` 又变回
「空气动力套件」。`fix_translation.py` 的 `--corrections` 已改成接受多个文件，后面的覆盖前面的：

```bash
--corrections chief_wave1_corrections.csv chief_wave2_corrections.csv chief_wave34_corrections.csv
```

只对最后一个（本波）文件报「未命中」——继承来的条目匹配不上是正常的，全报出来会淹没真正要看的。

#### ⚠️ 数字归一化踩过的坑

`GT300` 一度变成「GT3零」。**不是模型错译，是 `normalise_numerals` 的正则**：
`(?<![A-Za-z])(\d{1,2})` 在 `GT300` 里匹配到末尾的 `00` 并转成「零」。在修正表里写
`GT300,GT300` 也没用——归一化跑在人工修正**之后**。

正则已改成 `(?<![A-Za-z\d])`，同时排除字母和数字前缀。这类 bug 只在「字母+3 位数字」上触发，
前两波的语料里恰好没有这种模式，**换一批语料才暴露**。

#### 29 个没有台本的文件夹（已处理）

CrewChief 自带的 `subtitles.csv` 覆盖不到 29 个文件夹 / **179 个 wav**，autovoicepack 那份旧
inventory 里同样没有。台本是**顺着 C# 里的 folder 常量读触发条件反推**出来的，不是翻译英文录音，
所以措辞不必照抄英文。结论钉在 `make_chief_inventory.py` 的 `MANUAL_TEXT` / `DROP` 两张表里：

| | 文件夹 | wav | 说明 |
|---|---:|---:|---|
| **手写中文** | 17 | 104 | `--manual` 单独导出，已是中文，**不要再送进翻译**，直接进 TTS |
| **不做** | 12 | 75 | 见下 |

不做的 12 个分三类：`acknowledge/breath_in` 是吸气声不是语音；`strength_of_field*` 属
`Ratings.cs`，AC 不加载；其余是 VSC、`virtual_energy`、`rejoining`、`wheel_damage`
——要么 AC 没有这功能，要么 `.cs` 里零引用是死音频。

⚠️ **两条手写台词是要和数字拼接的片段，中文语序不同，装包后必须实听验证**：

| 文件夹 | 拼接顺序 | 中文 |
|---|---|---|
| `lap_counter/race_starts_in` | 前缀 + 数字 + `timings/seconds` | 比赛开始还有 + 三十 + 秒 |
| `overtaking_aids/drs_activations_remaining` | 数字 + 后缀 | 三 + 次 DRS 可用 |

### 第三阶段：完善（1 周）

```
⑧ UI 2222 条中文化
⑨ ColloquialTime 中文化
⑩ 语音指令（挑 30-50 个 + 装 zh-CN 引擎）
```

**总计约 1 人 4–6 周**，其中机器时间仅 3 小时，绝大部分是翻译与校对。

---

## 十、环境搭建（M1 实测）

Docker 镜像基于 `nvidia/cuda` + amd64，Apple Silicon 无法使用。venv 建在 autovoicepack 目录下（本库不放二进制依赖）：

```bash
cd ~/Projects/crew-chief-autovoicepack
python3 -m venv .venv
.venv/bin/pip install coqui-tts
.venv/bin/pip install "transformers>=4.57,<5"
.venv/bin/pip install "torch<2.9" "torchaudio<2.9"
.venv/bin/pip install jieba pypinyin
```

**四个依赖坑（按顺序踩到）：**

| 现象 | 原因 | 解法 |
|---|---|---|
| `No module named 'torch'` | coqui-tts 0.27 不再声明 torch 依赖（让用户自选 CUDA/CPU 版本） | 单独装 |
| `cannot import name 'isin_mps_friendly'` | transformers 5.x 移除了该函数 | 降到 4.57.x |
| `torchcodec library is required` | torch 2.9+ 强制要 torchcodec，而它又要系统装 ffmpeg | 降到 torch 2.8 规避 |
| XTTS 中文分支报错 | 缺中文 g2p 依赖 | 装 `jieba` `pypinyin` |

生成速度（M1 / MPS）：短句 1.6–2.1 秒/条，长句 2.5–3.2 秒/条。

### 翻译环境（ollama，第二阶段才需要）

XTTS 那套 venv 与 ollama 互不相干，装不装 ollama 不影响生成音频。

```bash
brew install ollama
brew services start ollama        # 或前台跑 ollama serve
ollama pull qwen2.5:14b           # 约 9GB
```

⚠️ **不要用 `qwen2.5:32b`**：19GB 模型在 16GB 内存的 M1 上会疯狂 swap。

`translate_phrases.py` 的 `--ollama_host` 默认是 `host.docker.internal:11434`（给 Docker 用的），
本机直接跑必须显式传 `localhost:11434`，否则连不上。

⚠️ XTTS v2 首次运行下载约 1.9GB 模型，需接受 **Coqui CPML 许可（非商用）**，`COQUI_TOS_AGREED=1` 可非交互接受。

---

## 十一、风险与未决项

| 风险 | 影响 | 应对 |
|---|---|---|
| **XTTS 中文音色带英文口音** | 听感 | 已知问题，Bart 音色可接受。彻底解决需真人中文录音做 baseline |
| **数字/时间拼接韵律** | 圈速、差距播报听起来生硬 | `NumberReaderZh` 必须先设计定稿再生成音频 |
| **短句不稳定** | 2–4 音节的 `安全`、`还在` 等 | XTTS 在极短文本上方差大，必要时单独重跑或调 `--xtts_speed` |
| **单变体机械感** | 同一句反复听会腻 | 原包每文件夹 2–7 个变体轮换，中文包可用 `--variation_count 1` 做双变体 |
| **XTTS 非商用许可** | 音频不能商用、不能改成 MIT | 免费开源分发可以，照 §13 的三个条件办；要商用则换 TTS 引擎（本方案的 inventory / 目录结构 / 后处理链均可复用） |
| **baseline 音色的来源条款** | 比 CPML 更容易被忽略 | Bart 是 ElevenLabs 合成音，见 §13.2。彻底规避只需重录 39 秒自己的 baseline |
| 中文语音识别率 | 语音指令可用性 | 砍到 30–50 个高频指令 |

---

## 十二、命令速查

路径约定：`$PACK` = 本库，`$AVP` = autovoicepack（提供 venv）。

```bash
export PACK=~/Projects/CrewChief_CN_VoicePack
export AVP=~/Projects/crew-chief-autovoicepack

# 工程师语料：先看波次构成，再切出某一波（§9 第二阶段）
cd $PACK && python3 scripts/make_chief_inventory.py --list_waves
python3 scripts/make_chief_inventory.py --wave 1 --out translations/chief_wave1.csv

# 翻译一波（ollama 需先起好，见 §10）
# ⚠️ 输出 CSV 若已存在会被当成断点续跑、用它预热缓存，改了 prompt 或术语表后必须先删掉
cd $AVP && .venv/bin/python translate_phrases.py \
  --target_language "Simplified Chinese" --model qwen2.5:14b \
  --phrase_inventory $PACK/translations/chief_wave1.csv \
  --translated_phrase_inventory $PACK/translations/chief_wave1_zh.csv \
  --glossary_file $PACK/translations/glossary_zh.txt \
  --ollama_host localhost:11434 --skip_sanity_check

# 生成
COQUI_TOS_AGREED=1 $AVP/.venv/bin/python $PACK/scripts/pilot_mac.py \
  --phrase_inventory $PACK/translations/phrase_inventory_zh.csv \
  --language zh-cn \
  --baseline_audio_dir $PACK/baseline/Bart \
  --output_audio_dir $PACK/output \
  --xtts_speed 1.45 --eq_preset radio --voice_name ChiefZH

# 小批量试点（17 条代表样本，约 40 秒）
COQUI_TOS_AGREED=1 $AVP/.venv/bin/python $PACK/scripts/pilot_mac.py \
  --phrase_inventory $PACK/translations/spotter_sample_zh.csv \
  --language zh-cn --baseline_audio_dir $PACK/baseline/Bart \
  --output_audio_dir $PACK/output \
  --xtts_speed 1.45 --eq_preset radio --voice_name Test

# 数字与时间：先验规则，再生成语料，最后生成音频（短句要多候选）
cd $PACK && python3 scripts/make_numbers_inventory.py --verify
python3 scripts/make_numbers_inventory.py --out translations/numbers_zh.csv
COQUI_TOS_AGREED=1 $AVP/.venv/bin/python $PACK/scripts/pilot_mac.py \
  --phrase_inventory $PACK/translations/numbers_zh.csv \
  --language zh-cn --baseline_audio_dir $PACK/baseline/Bart \
  --output_audio_dir $PACK/output \
  --xtts_speed 1.45 --eq_preset radio --attempts 5 --voice_name Numbers

# 应用修正表（术语归一化 → 人工修正 → 数字归一化），再并入手写中文那批
cd $PACK && python3 scripts/fix_translation.py \
  --in translations/chief_wave1_zh.csv \
  --corrections translations/chief_wave1_corrections.csv \
  --out translations/chief_wave1_final.csv
tail -n +2 translations/chief_wave1_manual_zh.csv >> translations/chief_wave1_final.csv

# 生成后体检
python3 scripts/qa_pack.py --phrase_inventory translations/numbers_zh.csv --pack output/Numbers

# 打包：把 output/ 下所有批次并成一棵 voice/ 树，产出 dist/crewchief-zh-voicepack-<版本>.zip
packaging/build_release.sh v0.1.0

# 把 autovoicepack 的改动回写本库留档
cd $AVP && git diff > $PACK/patches/autovoicepack-zh-cn-support.diff
```

`pilot_mac.py` 全部路径都走参数，所以脚本放本库、venv 留在 autovoicepack 不冲突。

### 现有产物

| 路径 | 内容 | 状态 |
|---|---|---|
| `translations/spotter_zh.csv` | Spotter 124 行，术语已定稿 | ✅ |
| `translations/spotter_sample_zh.csv` | 17 条代表样本，快速试听用 | ✅ |
| `translations/radio_check_zh.csv` | 无线电测试 4 行 | ✅ |
| `translations/numbers_zh.csv` | 数字与时间 138 行 | ✅ |
| `translations/pilot_zh.csv` | 最初 10 条试点 | ✅ |
| `translations/chief_wave1_final.csv` | 工程师波次 1，**1921 行，已审校定稿** | ✅ |
| `translations/chief_wave1_corrections.csv` | 波次 1 人工修正 404 条，按英文索引，跨波次通用 | ✅ |
| `translations/glossary_zh.txt` | 注入翻译 prompt 的赛车术语表，**119 条** | ✅ |
| `translations/chief_manual_zh.csv` | 官方没台本的 17 个文件夹，104 行手写中文，跳过翻译直接 TTS | ✅ |
| `translations/chief_wave2_final.csv` | 工程师波次 2，**1629 行，已审校定稿** | ✅ |
| `translations/chief_wave2_corrections.csv` | 波次 2 人工修正 212 条 | ✅ |
| `translations/chief_wave3_final.csv` | 工程师波次 3，**1543 行，已审校定稿** | ✅ |
| `translations/chief_wave4_final.csv` | 工程师波次 4，**151 行，已审校定稿** | ✅ |
| `translations/chief_wave34_corrections.csv` | 波次 3+4 人工修正 231 条 | ✅ |
| `scripts/make_chief_inventory.py` | 从官方 `subtitles.csv` 切工程师语料，按波次分批 | ✅ |
| `scripts/fix_translation.py` | 机翻后处理：术语归一化 + 人工修正 + 数字归一化 | ✅ |
| `baseline/Bart/` | 12 条 / 38.9 秒，定稿 baseline | ✅ |
| `scripts/pilot_mac.py` | 本地生成脚本（脱离 Docker/CUDA） | ✅ |
| `scripts/make_numbers_inventory.py` | 生成数字语料 + 朗读规则验证 | ✅ |
| `scripts/qa_pack.py` | 生成后体检：格式、削波、语速 | ✅ |
| `packaging/build_release.sh` | 合批 + 打 zip，分类重名直接报错退出 | ✅ |
| `packaging/INSTALL.txt` | 随 Release 分发的最终用户安装说明 | ✅ |
| `packaging/CPML.txt` | CPML 1.0.0 逐字存档，必须随包分发，见 §13.1 | ✅ |
| `src/NumberReaderZh.cs` | 中文数字朗读实现 | ✅ |
| `patches/autovoicepack-zh-cn-support.diff` | autovoicepack 全部改动 | ✅ |
| `patches/crewchief-zh-numberreader.diff` | CrewChief 侧改动 | ✅ |
| `output/Spotter_final/` | Spotter 124 + radio_check 4 | ✅ |
| `output/Numbers/` | 数字与时间 138 | ✅ |

### 生成脚本的两个中文专属参数

短文本（一两个汉字）是 XTTS 最不稳的地方 —— 会在音节前后补一段幻觉音，或者干脆把音节念两遍。
两道防线，都只对短句生效，长句不受影响：

| 参数 | 默认 | 作用 |
|---|---|---|
| `--artifact_trim_max_syllables` | 4 | 音节后若出现 ≥250ms 的近静音间隙，其后的内容判为幻觉音，裁掉 |
| `--attempts` / `--attempts_max_syllables` | 1 / 6 | 生成多个候选取最短的合格者。所有失败模式都让音频变长，所以最短的那个基本就是对的 |

数字包用 `--attempts 5` 生成。工程师那 3772 条大多是整句，稳定得多，默认 1 次即可。

---

## 十三、许可与分发

> 以下是对许可文本的阅读理解，不是法律意见。权威文本：[Coqui Public Model License 1.0.0](https://huggingface.co/coqui/XTTS-v2/raw/main/LICENSE.txt)。

### 13.1 CPML：开源不解除许可

常见误解是「开源了就不受约束」。**不成立。** CPML 定义的「使用」涵盖模型**及其输出**，
生成出来的音频跟模型权重受同一份许可管，与收不收钱无关。收费与否决定的是「你的用途算不算商用」，
不决定「许可管不管得着这些音频」。

好消息是 CPML 的非商用定义明确包含个人研究、私人娱乐、业余项目——**一个免费的游戏语音包落在这一侧，
可以公开发布**。三个硬条件：

| # | 条件 | 落实方式 | 状态 |
|---|---|---|---|
| 1 | **必须传递条款**：任何从你这里拿到副本的人也要拿到 CPML 全文 | `packaging/CPML.txt` 逐字存档，`build_release.sh` 打进 zip 根目录；文件缺失直接报错退出 | ✅ 已落实 |
| 2 | **音频不能改成 MIT**：CPML 不允许 sublicense 或转让许可 | README 与 INSTALL.txt 保持「语料与脚本 MIT / 生成音频受上游约束」的分层写法，别合并 | ✅ 已如此 |
| 3 | **下游同样不能商用** | 写明别人不能把本包塞进付费产品或付费整合包 | ✅ 已写明 |

边界提醒：接受赞助/捐赠、挂在靠广告变现的站点，都会往商业化那侧滑。Coqui 已于 2024 年 1 月关停，
既没人能卖商业许可，实际也没人执行——但「没人管」不等于「合法」，风险自负。

### 13.2 容易漏掉的第二层：baseline 音色的来源条款

Bart 不是 CPML 的问题，是**另一份条款**。autovoicepack 的 baseline 来自 ElevenLabs
（其 README 的 `record_elevenlabs_voice.py` 一节就是教你从 ElevenLabs 拉 baseline），
而 ElevenLabs 的服务条款通常禁止用其输出去训练或克隆到其他语音模型——
「拿它当 XTTS 的 voice cloning baseline」正好在这个措辞的射程内。

autovoicepack 仓库是 MIT，但 MIT 覆盖的是代码，覆盖不了上游服务的条款。这条与开不开源无关，
但公开分发会放大可见度。**分发前自行核对 ElevenLabs 现行条款；若要彻底规避，重录 39 秒自己的 baseline 即可**——
只换 `baseline/` 里那 12 个 wav，全链路其余部分不动。

### 13.3 要商用怎么办

换 TTS 引擎。语料 CSV、目录结构、`NumberReaderZh`、后处理链全部可复用，要换的只有 baseline 和推理那一步。
国产开源里 CosyVoice 2（阿里）等走 Apache-2.0 路线，但**具体权重的许可要自己逐个核实**，别信二手说法——
不少模型是「代码 Apache、权重 CC-BY-NC」的双轨制，跟 XTTS 是同一个坑。
